{
  "name": "nova-zombie-simulator",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "DEPLOY_ENV=GH_PAGES gh-pages --nojekyll -d dist",
    "publish": "npm run deploy",
    "open": "/usr/bin/open -a \"/Applications/Google Chrome.app\" https://michaelpaulukonis.github.io/nova-zombie-simulator"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "q5": "^4.7.5",
    "q5play": "^4.3.0"
  }
}
