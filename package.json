{
  "name": "p5js-vite",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "DEPLOY_ENV=GH_PAGES gh-pages -d dist",
    "publish": "npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "p5js-wrapper": "^1.2.3",
    "p5play": "^3.22.11",
    "planck": "^1.0.2"
  }
}
